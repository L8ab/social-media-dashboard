name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Validate
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Success
      run: echo "✅ Repository validated successfully"
